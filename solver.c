#include "fillit.h"

int		solve(int field_size, t_tet **tets)
{
	char	**matrix;
	//int		*stack;

	//field_size = 0;
	//tets = NULL;

	if ((matrix = get_matrix(field_size, *tets)) == NULL)
		return (-1);
	//if ((stack = ft_memaloc

	return (0);
}
